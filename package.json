{
  "name": "inocportfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://github.com/INOCcent-afk/inocportfolio",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "rm -rf dist/ && npm run build && gh-pages -d dist"
  },
  "author": "Dave Inoc",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.29.0"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "gsap": "^3.5.1"
  }
}
